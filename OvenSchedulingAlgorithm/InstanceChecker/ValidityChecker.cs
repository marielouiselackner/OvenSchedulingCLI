using OvenSchedulingAlgorithm.Interface;
using System;
using System.Collections.Generic;
using System.Text;

namespace OvenSchedulingAlgorithm.InstanceChecker
{
    class ValidityChecker
    {
        //TODO check basic stuff here, e.g. is list of machines non-empty? 
        //throw exceptions if something ist not right (I think no need to write to file)
        //TODO how can we check or ensure that e.g. the attribute of all jobs actually comes from the list of attributes?
        public static bool CheckValidity(IInstance instance)
        {
            throw new NotImplementedException();
        }
    }
}
